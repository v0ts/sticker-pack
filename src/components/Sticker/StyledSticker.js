import styled from "styled-components";

export const StyledSticker = styled.li`
    margin: 0 auto;

    & button {
        cursor: pointer;
        border-radius: 20px;
        border: solid 5px black;
        background-color: pink;
    }
`;